import BaseProvider from '../BaseClasses/BaseProvider'

class BranchProvider extends BaseProvider {
  // Not extended yet
}

const BranchesProvider = new BranchProvider('branches')

export default BranchesProvider
